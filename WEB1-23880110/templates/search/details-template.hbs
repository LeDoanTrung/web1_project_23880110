{{#each data}}
<h1>{{name}}</h1>
<div class="overflow-auto">
    {{#if imagepath}}
    <img src="{{imagepath}}" alt="{{name}}" class="float-start me-3">
    {{/if}}
    <div class="text-black-50">{{{description}}}</div>
</div>
{{/each}}